import { within, userEvent } from '@storybook/testing-library';

import App from '../App';

export default {
  title: 'components/App',
  component: App,
  parameters: {
    layout: 'fullscreen',
  },
};

export const Default = {};
